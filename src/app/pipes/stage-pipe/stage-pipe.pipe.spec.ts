import { StagePipePipe } from './stage-pipe.pipe';

describe('StagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StagePipePipe();
    expect(pipe).toBeTruthy();
  });
});
